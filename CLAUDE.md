# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository currently contains no source code — only a README stating the project name ("NCT" / "New Conditions Tech") and no further description. There is no build system, package manager, dependency manifest, test suite, or application code to reference yet.

Because there is nothing to build on, do not assume a language, framework, or architecture. When the user starts adding code, ask what stack/tooling they intend to use before scaffolding anything, and update this file with real commands (build/lint/test) and architecture notes once they exist.
